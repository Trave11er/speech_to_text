[project]
name = "sst"
version = "0.1.0"
description = "Minimal offline speech-to-text daemon for Linux"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "sounddevice",
    "numpy",
    "onnxruntime",
    "sherpa-onnx>=1.12.23",
]
