[project]
name = "sst"
version = "0.1.0"
description = "Minimal offline speech-to-text daemon for Linux"
readme = "README.md"
requires-python = ">=3.12, <3.14"
dependencies = [
    "sounddevice",
    "numpy",
    "sherpa-onnx>=1.12.23",
    "onnxruntime",
]
